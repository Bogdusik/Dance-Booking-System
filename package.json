{
  "name": "dance-organisation-webapp",
  "version": "1.0.0",
  "description": "Booking system for local dance organisation",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "npx kill-port 3000 && nodemon app.js",
    "test": "mocha tests/**/*.test.js --timeout 20000",
    "test:watch": "mocha tests/**/*.test.js --watch --timeout 10000",
    "test:coverage": "nyc mocha tests/**/*.test.js --timeout 10000"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.0",
    "connect-flash": "^0.1.1",
    "csurf": "^1.10.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "express-validator": "^7.2.1",
    "helmet": "^8.1.0",
    "mustache-express": "^1.3.2",
    "nedb": "^1.8.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "chai": "^5.2.0",
    "mocha": "^11.1.0",
    "nodemon": "^2.0.22",
    "nyc": "^15.1.0",
    "supertest": "^7.1.0"
  }
}
